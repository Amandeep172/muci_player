import 'package:flutter/material.dart';

class Songwave extends StatelessWidget {
  const Songwave({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(Icons.waves);
  }
}
